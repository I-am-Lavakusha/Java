# Java
This is my java repository and i will have all my java learnings in this repository

The java is an objext oriented programming language.

The features of java are
- Fast
- Robust
- object oriented
- secure
- statically typed
- platform independent
- compiled and interpreted language

The java is a compiled and interpreted language where it used compiler javac then the bytecode will get converted into machine code by the jit (just in time).

There are compiled languages like c and cpp
The c programming uses a gcc compiler where cpp used a g++ compiler.

There are programming languages they uses interpreter some popular one among them are python and javascript.
python uses the cpython interpreter for c environments and other.
javascript used v8 in chrome.

